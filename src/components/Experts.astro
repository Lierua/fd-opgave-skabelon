---
import { Image } from "astro:assets";
import Expert from "@assets/images/Experts.jpg";
import Tick from "@assets/images/Tick.svg";
import Pattern from "@assets/images/Pattern.png";
---

<div>
    <div class="content">
        <Image class="pattern" src={Pattern} alt="pattern" />
        <Image class="img" src={Expert} alt="Experts" />
        <div>
            <h3>WE'LL WORK WITH YOU</h3>
            <h2>What To Expect</h2>
            <p>Our collaboration is built on transparency, efficiency, and measurable results. Together, we create strategies tailored to your unique needs and goals.</p>
            <ul class="icon-list">
                <li>
                    <Image class="icon" src={Tick} alt="Tick" />
                    Collaborate to build actionable strategies that drive sustainable growth and deliver tangible outcomes.</li>
                <li>
                    <Image class="icon" src={Tick} alt="Tick" />
                    Identify opportunities to optimize processes and implement impactful solutions with measurable results.</li>
            </ul>
        </div>
    </div>
</div>

<style>
    .content {
        display: grid;
        gap: 3rem;
        grid-template-columns: minmax(0, 632px) 1fr;
        grid-template-rows: 20px 395px;
        margin-bottom: var(--margin-space);
        margin-top: var(--margin-space);
        position: relative;
            .pattern {
                position: absolute;
                scale: 1;
                left: 0;
                top: 0;
                z-index: -2;
            }
            &::after {
                top: 40px;
                left: -40px;
                content: "";
                position: absolute;
                height: 183px;
                width: 164px;
                border-radius: var(--border-radius);
                background-color: var(--green);
                z-index: -1;
            }
        .img {
            align-self: stretch;
            width: 100%;
            object-fit: cover;
            grid-row: 2;
            border-radius: var(--border-radius);
        }
        div {
            grid-row: 1/-1;
            > * + * {
                margin-block-start: 1rem;
            }
            h2 {
                font-weight: 600;
                font-size: var(--step-4);
            }
            h3 {
                font-size: var(--step--1);
                letter-spacing: 0.2rem;
            }
            p {
                text-wrap: pretty;
            }
            ul {
                list-style: none;
                padding: 0;
                margin: 0;
                margin-block-start: 2rem;
            }
            li {
                display: flex;
                align-items: center;
                gap: 0.5em;
                margin-bottom: 0.7rem;
                text-wrap: balance;
                    .icon {
                    width: 20px;
                    height: 20px;
                    flex-shrink: 0;
                    }
            }
        }
    }
</style>